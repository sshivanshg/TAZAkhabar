# Future apps live under /apps.
# Client: Expo universal app at apps/app (web + iOS + Android).
# API: apps/api (.NET 8).
#
# Do not add a separate Vite/CRA web app — web is exported from Expo
# (see docs/adr/003-expo-universal-client.md).
