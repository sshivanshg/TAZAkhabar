# Default to the hosted API so reader UI work does not require the .NET API,
# Postgres, or Docker on the developer machine.
EXPO_PUBLIC_API_BASE_URL=https://tazakhabar-140j.onrender.com
EXPO_PUBLIC_APP_ENV=production
